spring:
  application:
    name: configServer
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configurations
  profiles:
    active: native

server:
  port: 8080